---
import ShopLayout from '../../layouts/ShopLayout.astro';
import { getProductBySlug, PRODUCTS } from '../../services/productService.js';
import AddToCartButton from '../../components/react/AddToCartButton.jsx';

const { slug } = Astro.params;
const product = getProductBySlug(slug) || PRODUCTS[0];
const relatedProducts = PRODUCTS.filter((p) => p.category === product.category && p.id !== product.id).slice(0, 3);
---

<ShopLayout title={`${product.name} | TradeLogix`}>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 space-y-12">
    <!-- Breadcrumb -->
    <nav class="flex items-center gap-2 text-xs text-gray-400">
      <a href="/" class="hover:text-white">Home</a>
      <span>/</span>
      <a href="/shop" class="hover:text-white">Shop</a>
      <span>/</span>
      <a href={`/shop?category=${product.category}`} class="hover:text-white">{product.category}</a>
      <span>/</span>
      <span class="text-white font-semibold truncate">{product.name}</span>
    </nav>

    <!-- Main Detail Grid -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
      <!-- Gallery Showcase -->
      <div class="space-y-4">
        <div class="glass-panel p-4 rounded-3xl border border-white/10 overflow-hidden bg-gray-950">
          <img
            src={product.image}
            alt={product.name}
            onerror="this.onerror=null;this.src='https://images.unsplash.com/photo-1505740420928-5e560c06d30e?w=800&auto=format&fit=crop&q=80';"
            class="w-full h-[400px] sm:h-[480px] object-cover rounded-2xl"
          />
        </div>

        {product.gallery && product.gallery.length > 1 && (
          <div class="flex gap-4 overflow-x-auto pb-2">
            {product.gallery.map((imgUrl, idx) => (
              <button
                key={idx}
                class="w-20 h-20 rounded-xl overflow-hidden glass-panel border border-white/10 shrink-0 hover:border-brand-500 transition-all"
              >
                <img src={imgUrl} alt={`${product.name} thumbnail ${idx}`} class="w-full h-full object-cover" />
              </button>
            ))}
          </div>
        )}
      </div>

      <!-- Details & Actions -->
      <div class="space-y-6">
        <div>
          {product.badge && (
            <span class="px-3 py-1 rounded-full bg-brand-500/20 text-brand-300 text-xs font-bold uppercase tracking-wider border border-brand-500/30">
              {product.badge}
            </span>
          )}
          <h1 class="font-display font-extrabold text-3xl sm:text-4xl text-white tracking-tight mt-3">
            {product.name}
          </h1>

          <!-- Ratings & Stock -->
          <div class="flex items-center gap-4 mt-3 text-xs text-gray-400">
            <div class="flex items-center gap-1 text-amber-400 font-bold">
              <span>★</span>
              <span>{product.rating}</span>
              <span class="text-gray-500">({product.reviewCount} verified reviews)</span>
            </div>
            <span>•</span>
            <span class={`font-bold ${product.inStock ? 'text-emerald-400' : 'text-rose-400'}`}>
              {product.inStock ? `In Stock (${product.stockCount} available)` : 'Out of Stock'}
            </span>
          </div>
        </div>

        <!-- Price Display -->
        <div class="glass-panel p-4 rounded-2xl flex items-baseline gap-3 border border-white/5">
          <span class="font-display font-extrabold text-3xl text-white">₹{product.price.toFixed(2)}</span>
          {product.originalPrice && (
            <span class="text-base text-gray-500 line-through">₹{product.originalPrice.toFixed(2)}</span>
          )}
          {product.originalPrice && (
            <span class="px-2 py-0.5 rounded bg-emerald-500/20 text-emerald-400 text-xs font-bold">
              Save ₹{(product.originalPrice - product.price).toFixed(2)}
            </span>
          )}
        </div>

        <p class="text-gray-300 text-sm leading-relaxed">
          {product.description}
        </p>

        <!-- React Island: Variant Selector & Add to Cart -->
        <div class="pt-4 border-t border-white/10">
          <AddToCartButton client:load product={product} showVariantSelector={true} />
        </div>

        <!-- Technical Specifications Table -->
        {product.specs && (
          <div class="pt-6 border-t border-white/10 space-y-3">
            <h3 class="font-display font-bold text-base text-white">Technical Specifications</h3>
            <div class="glass-panel rounded-2xl p-4 border border-white/5 space-y-2 text-xs">
              {Object.entries(product.specs).map(([key, val]) => (
                <div key={key} class="flex justify-between py-1 border-b border-white/5 last:border-0">
                  <span class="text-gray-400 font-medium">{key}</span>
                  <span class="text-white font-semibold">{val}</span>
                </div>
              ))}
            </div>
          </div>
        )}
      </div>
    </div>

    <!-- Related Products -->
    {relatedProducts.length > 0 && (
      <div class="pt-16 border-t border-white/10 space-y-8">
        <h2 class="font-display font-bold text-2xl text-white">You Might Also Like</h2>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
          {relatedProducts.map((rel) => (
            <div class="glass-panel glass-panel-hover rounded-3xl p-4 flex flex-col justify-between group">
              <a href={`/shop/${rel.slug}`} class="block overflow-hidden rounded-2xl mb-4 bg-gray-900">
                <img src={rel.image} alt={rel.name} class="w-full h-44 object-cover group-hover:scale-105 transition-transform" />
              </a>
              <h3 class="font-display font-semibold text-sm text-white truncate mb-2">{rel.name}</h3>
              <div class="flex items-center justify-between">
                <span class="font-bold text-white text-base">₹{rel.price.toFixed(2)}</span>
                <AddToCartButton client:load product={rel} compact={true} />
              </div>
            </div>
          ))}
        </div>
      </div>
    )}
  </div>
</ShopLayout>
