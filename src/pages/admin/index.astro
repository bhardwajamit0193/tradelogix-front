---
import AdminLayout from '../../layouts/AdminLayout.astro';
import AnalyticsOverview from '../../components/react/AnalyticsOverview.jsx';
import { getOrders } from '../../services/orderService.js';

const recentOrders = getOrders().slice(0, 4);
---

<AdminLayout title="Admin Overview | TradeLogix Studio" headerTitle="Executive Dashboard" headerSubtitle="Real-time sales performance, active metrics, and recent order stream">
  <div class="space-y-8">
    <!-- React Island: Metrics Overview & Charts -->
    <AnalyticsOverview client:load />

    <!-- Recent Orders Snapshot -->
    <div class="glass-panel p-6 sm:p-8 rounded-3xl border border-slate-200 bg-white shadow-sm space-y-6">
      <div class="flex items-center justify-between">
        <div>
          <h3 class="font-display font-bold text-xl text-slate-900">Recent Customer Orders</h3>
          <p class="text-xs text-slate-500">Latest incoming purchases awaiting fulfillment</p>
        </div>
        <a href="/admin/orders" class="text-xs font-semibold text-brand-600 hover:text-brand-700 flex items-center gap-1">
          Manage All Orders →
        </a>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {recentOrders.map((ord) => (
          <div class="glass-panel p-4 rounded-2xl border border-slate-200 bg-slate-50 space-y-2 text-xs">
            <div class="flex items-center justify-between">
              <span class="font-mono font-bold text-brand-600">{ord.id}</span>
              <span class="px-2 py-0.5 rounded text-[10px] font-bold uppercase bg-brand-50 text-brand-700 border border-brand-200">
                {ord.status}
              </span>
            </div>
            <p class="font-semibold text-slate-900 truncate">{ord.customerName}</p>
            <div class="flex items-center justify-between text-slate-500 pt-2 border-t border-slate-200">
              <span>{ord.date}</span>
              <span class="font-display font-extrabold text-slate-900 text-sm">₹{ord.total.toFixed(2)}</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</AdminLayout>
