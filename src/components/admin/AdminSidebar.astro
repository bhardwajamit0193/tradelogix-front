---
const { currentPath = '/admin' } = Astro.props;

const navItems = [
  { label: 'Overview', path: '/admin', icon: 'M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6' },
  { label: 'Products Catalog', path: '/admin/products', icon: 'M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4' },
  { label: 'Customer Orders', path: '/admin/orders', icon: 'M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z' },
  { label: 'Customers', path: '/admin/users', icon: 'M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z' },
];
---

<aside class="w-64 bg-gray-950 border-r border-white/10 flex flex-col justify-between shrink-0 min-h-screen">
  <div class="p-6 space-y-8">
    <!-- Brand Header with logo.jpeg -->
    <a href="/admin" class="flex items-center gap-3">
       <img
        src="/logo-white.png"
        alt="TradeLogix Logo"
        class="h-14 w-auto  object-contain  group-hover:scale-105 transition-transform"
      />
    </a>

    <!-- Navigation Menu -->
    <nav class="space-y-1.5">
      <p class="px-3 text-[10px] font-bold text-gray-500 uppercase tracking-wider mb-2">Main Menu</p>
      {navItems.map((item) => {
        const isActive = currentPath === item.path;
        return (
          <a
            href={item.path}
            class={`flex items-center gap-3 px-3.5 py-2.5 rounded-xl text-xs font-semibold transition-all ${
              isActive
                ? 'bg-brand-500/20 text-brand-300 border border-brand-500/30 shadow-glow-primary'
                : 'text-gray-400 hover:text-white hover:bg-white/5'
            }`}
          >
            <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={item.icon} />
            </svg>
            <span>{item.label}</span>
          </a>
        );
      })}
    </nav>
  </div>

  <!-- Bottom Return Button -->
  <div class="p-6 border-t border-white/10 space-y-3">
    <a
      href="/"
      class="flex items-center justify-center gap-2 w-full py-2.5 px-4 rounded-xl bg-gray-900 border border-white/10 text-gray-300 hover:text-white text-xs font-semibold hover:border-white/20 transition-all"
    >
      <svg class="w-4 h-4 text-brand-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
      </svg>
      Back to Storefront
    </a>
  </div>
</aside>
